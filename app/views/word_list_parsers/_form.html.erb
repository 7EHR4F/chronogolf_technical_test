<%= form_for(word_list_parser) do |f| %>
  <% if word_list_parser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_list_parser.errors.count, "error") %> prohibited this word_list_parser from being saved:</h2>

      <ul>
      <% word_list_parser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label 'name' %>
    <%= f.text_field :name %>
    <%= f.file_field :wordlist %>
    <%= f.submit %>
  </div>
<% end %>
